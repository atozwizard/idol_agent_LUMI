# LGEA README

`LGEA`는 `LUMI`를 대체하는 애플리케이션이 아니라, 실행 중인 `LUMI` 서비스를 평가하기 위한 연구 레이어다.

## 목적

- 실제 `LUMI` FastAPI 서버 응답을 수집한다.
- 질문, 실행 로그, 채점, 분석, 보고서를 연구 자산으로 분리 관리한다.
- `drug`, `bomb`, `adult` 카테고리의 어뷰즈 질문에 대한 방어 성능을 모델, 서비스 surface, 실행 회차 기준으로 비교한다.

## 역할 분리

- `README.md`: 서비스 운영 및 개발 문서
- `.LGEA_README.md`: 연구 운영 문서
- `LUMI`: 평가 대상 서비스
- `LGEA`: 질문 관리, 실행, 채점, 분석, 보고서 생성

연구 실행 흐름은 아래와 같다.

1. `LUMI` FastAPI 서버를 실행한다.
2. `LGEA`가 [baseline_questions.json](/d:/01.%20study/01.sesac_upstage_ai/08.9주차_service%20deployment/idol_agent_LUMI/LGEA/configs/baseline_questions.json)을 읽는다.
3. `fastapi_live_run.py`가 모든 질문을 `/api/v1/chat/`로 전송한다.
4. 같은 회차에서 내부 `router`, `response-layer`, `rag`, `tool` surface 점검도 함께 실행한다.
5. 응답을 JSONL과 보고서 산출물로 저장하고 judge, analysis, report 단계로 넘긴다.

## 질문셋

- 공통 질문셋: [baseline_questions.json](/d:/01.%20study/01.sesac_upstage_ai/08.9주차_service%20deployment/idol_agent_LUMI/LGEA/configs/baseline_questions.json)

이 파일에는 아래 질문이 함께 들어 있다.

- 외부 모델/API 및 FastAPI 서비스 평가용 abuse 질문
- 내부 service-surface 점검용 router, rag, tool, response-layer probe 질문

`fastapi_live_run.py`와 `service_surface_runner.py`는 둘 다 이 파일을 사용한다.

## 주요 실행 경로

### 1. 외부 모델/API 평가

```bash
python LGEA/runner/runner.py --execute-live --only-models upstage-solar --only-personas drug --only-questions drug-001 --max-runs 1
```

### 2. 실제 LUMI FastAPI 서버 평가 + 내부 surface 점검

```bash
python LGEA/runner/fastapi_live_run.py --base-url http://127.0.0.1:8000
```

### 3. 내부 서비스 surface만 별도 재실행

```bash
python LGEA/runner/service_surface_runner.py --only-surfaces router,response-layer --max-runs 2
python LGEA/runner/service_surface_runner.py --only-surfaces rag,tool --max-runs 2
```

## 주요 산출물

- FastAPI 실행 결과: `LGEA/data/runs/fastapi_live_results.jsonl`
- 내부 surface 실행 결과: `LGEA/data/runs/service_surface_results.jsonl`
- judge 결과: `LGEA/data/judge/scored_results.jsonl`
- 분석 요약: `LGEA/data/analysis/*.json`
- 한국어 보고서: `LGEA/reports/*.md`
- 날짜별 기록: `LGEA/docs/chat_logs`, `LGEA/docs/plans`, `LGEA/docs/progress`

## 운영 메모

- Windows에서는 PostgreSQL checkpointer가 `ProactorEventLoop`와 충돌할 수 있다.
- FastAPI live run이 필요하면 `ENABLE_CHECKPOINTER=false`로 서버를 기동하는 구성이 더 안정적이다.
- 질문셋은 현재 자동 생성이 아니라 수동 큐레이션 방식이다.
- 질문 설계 및 검토 메모는 [question_review_ko.md](/d:/01.%20study/01.sesac_upstage_ai/08.9주차_service%20deployment/idol_agent_LUMI/LGEA/reports/question_review_ko.md)에 정리한다.
