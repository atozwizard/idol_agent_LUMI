# LGEA README

`LGEA`는 `LUMI`를 대체하는 앱이 아니라, 실행 중인 `LUMI` FastAPI와 내부 service surface를 평가하는 연구 레이어다.

## 목적

- 실제 `LUMI` FastAPI 응답을 수집한다.
- 질문, 실행 로그, 채점, 분석, 보고서를 연구 자산으로 분리 관리한다.
- `drug`, `bomb`, `adult` 어뷰즈 질문과 benign probe 질문에 대해 종단 응답과 내부 표면을 함께 비교한다.

## 역할 분리

- `LUMI`: 평가 대상 서비스
- `LGEA`: 질문 관리, 실행, 채점, 분석, 보고서 생성

## 공통 질문셋

- 기준 질문셋: [baseline_questions.json](/d:/01.%20study/01.sesac_upstage_ai/08.9주차_service%20deployment/idol_agent_LUMI/LGEA/configs/baseline_questions.json)

이 파일에는 다음이 함께 들어 있다.
- FastAPI 종단 평가용 abuse 질문
- 내부 `router`, `response-layer`, `rag`, `tool` probe 질문

## 단일 실행 진입점

아래 명령을 기준 실행 경로로 사용한다.

```bash
python LGEA/runner/fastapi_live_run.py --base-url http://127.0.0.1:8000
```

이 명령은 기본적으로 다음 단계를 순서대로 수행한다.

1. `baseline_questions.json` 전체 질문을 실제 `/api/v1/chat/`에 전송
2. 같은 질문셋으로 내부 service surface 점검 실행
3. FastAPI 결과 보고서 생성
4. service surface 결과 보고서 생성
5. `solar-pro2` 기반 LLM judge 실행 시도
6. `solar-pro2` 기반 질문 확장기 실행 시도
7. cross-surface analysis 생성
8. comparative analysis 생성
9. repetition plan 갱신
10. thesis-ready report 생성

참고:
- `solar-pro2` 관련 단계는 네트워크/API 상태에 따라 실패할 수 있지만, FastAPI 본 실행 결과는 유지된다.
- 해당 단계만 건너뛰고 싶으면 `--skip-llm-judge`, `--skip-question-expander`, `--skip-analysis`, `--skip-service-surface-checks`를 사용할 수 있다.

## 주요 산출물

- FastAPI 실행 결과: `LGEA/data/runs/fastapi_live_results.jsonl`
- 내부 surface 실행 결과: `LGEA/data/runs/service_surface_results.jsonl`
- LLM judge 결과: `LGEA/data/judge/llm_scored_results.jsonl`
- 질문 확장 결과: `LGEA/data/questions/stress_expanded_questions.json`
- 분석 요약: `LGEA/data/analysis/*.json`
- 보고서: `LGEA/reports/*.md`
- 날짜별 기록: `LGEA/docs/chat_logs`, `LGEA/docs/plans`, `LGEA/docs/progress`

## 운영 메모

- Windows에서는 PostgreSQL checkpointer가 `ProactorEventLoop`와 충돌할 수 있다.
- FastAPI live run 전 `ENABLE_CHECKPOINTER=false`로 서버를 띄우는 구성이 더 안정적이다.
- 논문용 주 경로는 원본 질문셋 기반 FastAPI 실행과 내부 surface 비교 결과다.
